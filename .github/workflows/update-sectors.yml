name: Update Sector Data

on:
  schedule:
    # Mon, Wed, Fri at 22:00 UTC  (after US market close ~17:00 ET)
    - cron: '0 22 * * 1,3,5'
  # Allow manual trigger from GitHub UI
  workflow_dispatch:

jobs:
  update-sectors:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-yfinance-supabase
          restore-keys: ${{ runner.os }}-pip-

      - name: Install dependencies
        run: pip install yfinance supabase pandas

      - name: Run sector update
        env:
          SUPABASE_URL:         ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/update_sectors.py
